@model IEnumerable<Barberia.Models.cliente>

@{
    ViewData["Title"] = "Index";
}

<style>
    .page-title {
        font-weight: 700;
        color: #1e1e1e;
        border-left: 5px solid #ffc107;
        padding-left: 12px;
        margin-bottom: 20px;
    }

    .btn-custom-primary {
        background-color: #ffc107;
        border: none;
        color: #000;
        font-weight: 600;
        padding: 10px 18px;
        border-radius: 10px;
        transition: .2s ease;
    }

        .btn-custom-primary:hover {
            background-color: #ffb300;
        }

    .table-custom {
        width: 100%;
        border-collapse: collapse;
        background-color: #ffffff;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 2px 6px rgba(0,0,0,.1);
    }

        .table-custom thead {
            background-color: #f0f0f0;
            font-weight: 600;
        }

        .table-custom th,
        .table-custom td {
            padding: 14px;
            border-bottom: 1px solid #e0e0e0;
            color: #1e1e1e;
        }

        .table-custom tbody tr:hover {
            background-color: #fff4d0;
            transition: .2s;
        }

    .action-links a {
        color: #000;
        font-weight: 500;
        margin-right: 8px;
        text-decoration: none;
    }

        .action-links a:hover {
            color: #ffc107;
        }
</style>

<h1 class="page-title">Clientes</h1>

<p>
    <a asp-action="Create" class="btn-custom-primary shadow-sm">Nuevo Usuario</a>
</p>

<table class="table-custom">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.NombreCliente)</th>
            <th>@Html.DisplayNameFor(model => model.Apellido)</th>
            <th>@Html.DisplayNameFor(model => model.Telefono)</th>
            <th>@Html.DisplayNameFor(model => model.Email)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.NombreCliente)</td>
                <td>@Html.DisplayFor(modelItem => item.Apellido)</td>
                <td>@Html.DisplayFor(modelItem => item.Telefono)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td class="text-center action-buttons">
                    <a asp-action="Edit" asp-route-id="@item.ClienteId" class="btn btn-sm btn-outline-primary me-1">
                        <i class="fa fa-edit"></i>
                    </a>
                    <a asp-action="Details" asp-route-id="@item.ClienteId" class="btn btn-sm btn-outline-secondary me-1">
                        <i class="fa fa-eye"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.ClienteId" class="btn btn-sm btn-outline-danger">
                        <i class="fa fa-trash"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
